{
  "name": "migcommit",
  "version": "0.1.0",
  "description": "Database migration tool that works like Git. Commit, push, pull, and track your database schema using plain SQL. Supports multiple environments and database engines.",
  "license": "ISC",
  "author": "will-berth",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/pg": "^8.15.4",
    "ts-node": "^10.9.2",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "node-sql-parser": "^5.3.10",
    "pg": "^8.16.2"
  },
  "bin": {
    "migcommit": "./dist/bin/cli.js"
  }
}
